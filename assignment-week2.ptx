<!--
  This assignment was used for the Fall 2020 term.
-->
<section xml:id="section-assignment-week2">
  <title>Week 2 Assignment</title>
  <p><ol>
    <li>
      <p>
      For each of the following situations, what kind of function
      might you choose to encode the dependence?  Give reasons for
      your choices. (8)
      <ol>
        <li>
          Precipitation rate in Edmonton as a function of time in
          months.
        </li>
        <li>
          Moose population as a function of number of approved hunting
          licenses.
        </li>
        <li>
          River flow rate in the spring as a function of temperature.
        </li>
        <li>
          Probability of flipping heads at least once as a function of
          number of coin flips.
        </li>
      </ol></p>
    </li>
    <li>
      <p>
      For the following pairs of functions <m>f</m> and <m>g</m>,
      write <m>f \circ g</m> and <m>g \circ f</m>.  Then state the
      domains of <m>f</m>, <m>g</m>, <m>f \circ g</m> and <m>g \circ
      f</m>.  (16)
      <ol>
        <li>
          <m>f(x) = \ln x</m> and <m>g(x) = e^ x</m>.
        </li>
        <li>
          <m>f(x) = x^3 + 4x^2 + x</m> and <m>g(x) = x^2</m>.
        </li>
        <li>
          <m>f(x) = \frac{1}{x}</m> and <m>g(x) = \frac{x}{1+x}</m>. 
        </li>
        <li>
          <m>f(x) = e^x</m> and <m>g(x) = \sin(6x)</m>.
        </li>
      </ol></p>
    </li>
    <li>
      <p>
      Invert the following functions on the given domain.  (You may
      assume the function is monotonic on the given domain.) State the
      domain of the inverse. (16)
      <ol>
        <li>
          <m>f(x) = \sin(4x)</m> on the domain
          <m>\left[\frac{-\pi}{8}, \frac{\pi}{8} \right]</m>.
        </li>
        <li>
          <m>f(x) = e^{-x^2}</m> on the domain <m>[0, \infty)</m>.
        </li>
        <li>
          <m>f(x) = (x-2)^2 + 5</m> on the domain <m>[2,
          \infty)</m>.
        </li>
        <li>
          <m>f(x) = \ln (1 + e^{x^3})</m> on the domain <m>\RR</m>.
        </li>
      </ol></p>
    </li>
  </ol></p>
</section>

